# Tier 2-Finance — receipt/invoice retrieval allowlist (spec 2026-06-09).
#
# The users below may retrieve FINANCIAL DOCUMENTS (receipts, invoices,
# statements, order confirmations, payments) from ANY monitored mailbox —
# but ONLY inside the #founder-finance channel, and ONLY financial documents.
# Outside that channel they are normal Tier-2 users (own mailbox only).
# The safety model is a CONTENT-TYPE permission, not a mailbox permission.
#
# Every cross-mailbox pull is written to logs/finance-access-audit.jsonl.
#
# FAIL-CLOSED: missing/unreadable file -> nobody has the finance power.

users:
  - U0B3AEJCYGP   # Justin Moran
  - U0B3PRZMBCN   # Eric Canku
  - U0B4L7886PJ   # Jerry Reick

# The ONE channel where the finance retrieval path exists.
# Repointed 2026-07-08 to #founder-finance (was #hjr-finance C0BAK65N4TA).
# Repointing this key ALSO moves the Tier-2-Finance cross-mailbox retrieval
# permission -- a deliberate security-boundary decision, not a rename.
channel_id: C0BCXPJDP42
channel_name: founder-finance

# Auto-file destination: "Receipts & Invoices Inbox" Drive folder.
drive_folder_id: "1I7zWcCIAOx7zdzIXcxx6WTLk1K40eizj"
