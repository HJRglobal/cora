# Meeting Action Capture -> Asana project routing
# ------------------------------------------------------------------------------
# Every Fireflies-captured action item is created INTO the mapped project so it
# is no longer an untaggable orphan in the assignee's My Tasks, and so the
# Entity / Status / Priority custom fields below can be set (Asana custom fields
# are project-scoped -- a task can only carry a field its project has attached).
#
# HOW TO POPULATE (one-time, by Harrison):
#   For each entity below, set project_gid to an Asana project that ALREADY has
#   the Entity / Status / Priority custom fields attached. Two good options:
#     (a) the entity's existing ops/goals project, or
#     (b) a dedicated "[ENTITY] Meeting Action Items" project you create.
#   Find a project's GID in its Asana URL: .../project/<PROJECT_GID>/...
#
# Until a project_gid is filled in, captured tasks for that entity fall back to
# the previous behavior (workspace-only -> lands in assignee My Tasks) and a
# warning is logged. No tasks are lost.
#
# custom_fields are set best-effort AFTER creation; a field not attached to the
# chosen project is logged and skipped -- it never aborts task creation.
# Field/option GIDs below are the canonical FNDR portfolio fields.

custom_fields:
  entity_field_gid: "1214487026542596"
  status_field_gid: "1214566926973275"
  status_not_started_option: "1214566926973276"
  priority_field_gid: "1204547177535963"
  priority_medium_option: "1204547177535965"
  # Entity is an enum field: each entity (route) code maps to a DIFFERENT option
  # GID. A captured task is tagged with its route-entity's Entity option, plus
  # Status=Not Started + Priority=Medium (above), at creation.
  #
  # ENABLED 2026-06-16 (Phase 1.10): the Entity/Status/Priority fields were
  # attached to the 10 capture catch-all projects via
  # scripts/attach_capture_custom_fields.py (design/asana-architecture.md Sec 5),
  # and the option GIDs below were read live from the Entity custom field
  # (1214487026542596). Tagging activates at the next capture fire (script-side,
  # no bot restart). set_task_custom_fields stays fail-safe: a field not on a
  # task's project is skipped, never fatal. (HJRP-RR has no Entity option, so its
  # captured tasks get Status/Priority only.)
  entity_options:
    FNDR: "1214487026542597"
    HJRG: "1214487026542598"
    F3E: "1214487026542599"
    F3C: "1214487026542600"
    UFL: "1214487026542601"
    POD: "1214487026542602"
    HJRPROD: "1214566927301309"
    HJRP: "1214487026542603"
    BDM: "1214487026542604"
    OSN: "1214487026542608"
    LEX: "1214566588327157"
    LEX-LLC: "1214487026542605"
    LEX-LLA: "1214487026542606"
    LEX-LBHS: "1214487026542607"

# entity code -> Asana project GID to drop captured tasks into.
# Populated 2026-06-06 with per-entity catch-all "Operations — General" project
# GIDs from asana-project-map.yaml (post Asana structure rebuild). Cross-checked:
# every GID equals that entity's catch_all_gid in asana-project-map.yaml.
# BDM is intentionally left empty -- excluded from the meeting-capture initiative.
# NOTE: LEX* entries are populated for completeness, but the capture extractor
# skips ALL LEX meetings (PHI guardrail) before project routing, so they're inert
# unless that guardrail is ever relaxed.
projects:
  F3E:      "1215470928454227"
  OSN:      "1215470834881074"
  HJRG:     "1215470834914137"
  UFL:      "1211401911685190"
  HJRP:     "1212895000591057"
  BDM:      ""
  HJRPROD:  "1215470944131060"
  FNDR:     "1215470834914137"
  F3C:      "1214489612964340"
  LEX:      "1215470944114390"
  LEX-LLC:  "1215470944114390"
  LEX-LLA:  "1215470928477598"
  LEX-LBHS: "1209152923740511"

# ------------------------------------------------------------------------------
# 2026-06-08 Asana governance restructure — new Harrison-owned projects.
# ------------------------------------------------------------------------------
# 11 new Harrison-owned projects replace old Hannah-owned F3E projects and
# untagged HJRP/OSN projects. They are FUNCTION-SPECIFIC (sales, leasing, tech,
# etc.), NOT per-entity catch-alls, so the meeting-capture `projects:` map above
# intentionally still points each entity at its "Operations — General" catch-all
# (the single drop target for un-routable captured action items). Functional
# routing for these new projects lives in asana-project-map.yaml
# (keyword_rules + meeting_title_rules). This block registers the new GIDs so
# the capture config reflects the new structure; old project GIDs are retained
# in asana-project-map.yaml until task migration is confirmed complete.
new_projects_2026_06_08:
  F3E:
    - gid: "1215477882565190"  # [F3E] Sales — Wholesale & Retail Pipeline
    - gid: "1215477795747654"  # [F3E] Sales — Account Management
    - gid: "1215477504148494"  # [F3E] Planning — 2026 Annual
    - gid: "1215477907120289"  # [F3E] Activations — Events & Sampling
    - gid: "1215477907334373"  # [F3E] Weekly — Team Meeting
    - gid: "1215477503091206"  # [F3E] Sales — Distributor Relations
  HJRP:
    - gid: "1215477882706087"  # [HJRP] Leasing — Active Leases & Renewals
    - gid: "1215477640975218"  # [HJRP] Maintenance — Inspections & Repairs
  OSN:
    - gid: "1215498953914619"  # [OSN] Tech — POS & Systems
    - gid: "1215477549068105"  # [OSN] Vendor — Good Pudding
  LEX:
    - gid: "1215477796340344"  # [LEX] Strategic — 2026 Planning

# ------------------------------------------------------------------------------
# Extra projects to receive the Entity/Status/Priority custom fields (WS10).
# scripts/attach_capture_custom_fields.py attaches the fields to the catch-alls
# AND new_projects_2026_06_08 above AND every GID listed here. Use this for active
# projects created later that lack the fields (e.g. TikTok Shop, Wikipedia
# Presence, the press pipeline). Add `- gid: "..."  # [LABEL]` rows, then run the
# script --dry-run (Harrison reviews) then live. NEVER list a BDM client project.
field_target_projects: []
