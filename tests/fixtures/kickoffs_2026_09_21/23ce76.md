```markdown
Model: Opus-tier — follow the STANDING OPERATING LOOP.

**AUTO-GENERATED DRAFT -- VERIFY-FIRST everything**

Suggested branch: `claude/redact-api-token-shapes-asana-pat-2-digits-digit`

---

## 0. Evidence
- The repo pre-commit hook already has the Asana PAT regex — reuse/import from there where feasible rather than re-deriving, to keep patterns in sync.
- Purge job should be run once post-deploy, then can be retired or scheduled as a periodic safety sweep — confirm with Harrison which cadence is wanted.
- No existing evidence pointer to the exact ch
