```markdown
# Cora Code Session: Staged code-session prompt cards not visually updating after button press

**Model: Opus-tier · STANDING OPERATING LOOP applies · Suggested branch: `claude/staged-code-session-prompt-cards-the-keep-park-d`**

> **AUTO-GENERATED DRAFT -- VERIFY-FIRST everything**

---

## 0. Evidence
- Investigate the referenced "previously logged pattern" (action executes, display doesn't refresh) as it may point to a shared root cause elsewhere in the codebase -- flag if found for a broader follow-up.
- Only one queued item in this batch; no additional slices needed.
```
