# F3 Community -- Known Answers

_Compiled from Cora knowledge-gap reviews. Read alongside CLAUDE.md and founder CLAUDE.md. Append-only -- manually consolidate if it gets noisy._
_Fallback store: the live bot reads KNOWN_ANSWERS_DIR (Drive _brain/known-answers/); this repo copy is the fallback used when that env var is unset. Created 2026-07-03 (audit W11-03) so an F3C known-answer read never silently returns empty._

## Routing rules

(empty - rules added as Harrison flags ROUTE: entries in digests)

## Known facts

(empty - facts added as Harrison fills answer blocks in digests)
